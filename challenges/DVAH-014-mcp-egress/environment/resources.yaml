# The MCP stub performs no real I/O, so no seeded backing store is needed. The
# attacker-controlled destination the exploit aims at:
egress:
  attacker_host: "evil.example"
  approved_host: "api.github.com"
