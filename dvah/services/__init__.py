"""In-process simulated external systems (M1).

M2 promotes these to real FastAPI apps over HTTP; the in-memory stores here remain
the seedable backend so behavior stays identical across transports.
"""
